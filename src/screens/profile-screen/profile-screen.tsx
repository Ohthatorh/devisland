import { FC } from "react";
import { StyleSheet, View, Text } from "react-native";

interface IProfileScreen {}

export const ProfileScreen: FC<IProfileScreen> = () => {
  return (
    <View>
      <Text>ProfileScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
