import { FC } from "react";
import { StyleSheet, View, Text } from "react-native";

interface IFavoritesScreen {}

export const FavoritesScreen: FC<IFavoritesScreen> = () => {
  return (
    <View>
      <Text>FavoritesScreen</Text>
    </View>
  );
};

const styles = StyleSheet.create({});
